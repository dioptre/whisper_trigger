[package]
name = "rust-audio"
version = "0.1.0"
authors = ["Mirror Mirror"]
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
wasm-bindgen = "0.2"

[profile.release]
opt-level = 3
lto = true
